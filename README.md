# DevDigest Tracker

Веб-приложение для агрегации и автоматической суммаризации контента из Telegram-каналов и RSS-фидов по программированию.

## Возможности

- 📱 **Telegram & RSS** — парсинг публичных каналов и RSS-фидов
- 🤖 **AI Саммари** — автоматическая генерация ежедневных дайджестов с помощью GPT-4
- 🎯 **Персонализация** — фильтрация по темам (React, TypeScript, DevOps и т.д.)
- 📊 **Dashboard** — удобный интерфейс для просмотра постов и саммари
- ⚡ **Next.js 16** — использование новейших фич (PPR, Server Actions, Cache Components)

## Технологический стек

- **Frontend**: Next.js 16, TypeScript, Tailwind CSS, shadcn/ui
- **Backend**: Next.js Server Actions, PostgreSQL, Prisma ORM
- **AI**: OpenAI GPT-4
- **Парсинг**: Telegram Bot API, RSS Parser
- **Кэш**: Redis
- **Инфраструктура**: Docker, Vercel

## Быстрый старт

### Требования

- Node.js 18+
- pnpm 8+
- Docker и Docker Compose

### Установка pnpm (если не установлен)

```bash
npm install -g pnpm

# Или через npm
npm install -g pnpm

# Или через Homebrew (macOS)
brew install pnpm
```

### Запуск проекта

1. **Клонировать репозиторий (или инициализировать проект)**

```bash
# Если еще нет Next.js проекта
pnpm create next-app@latest . --typescript --tailwind --app

# Или если проект уже существует, просто установить зависимости
pnpm install
```

2. **Запустить Docker контейнеры**

```bash
# Запустить PostgreSQL и Redis
docker-compose up -d

# Проверить статус
docker-compose ps
```

3. **Настроить переменные окружения**

```bash
# Скопировать файл примера
cp .env.example .env.local

# Отредактировать .env.local и добавить:
# - NEXTAUTH_SECRET (сгенерировать: openssl rand -base64 32)
# - TELEGRAM_BOT_TOKEN (получить у @BotFather)
# - OPENAI_API_KEY (получить на platform.openai.com)
```

4. **Установить зависимости**

```bash
pnpm install
```

5. **Настроить базу данных**

```bash
# Инициализировать Prisma (если нужно)
pnpm exec prisma init

# Применить миграции
pnpm exec prisma migrate dev --name init

# Сгенерировать Prisma Client
pnpm exec prisma generate
```

6. **Запустить dev сервер**

```bash
pnpm dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Команды

### Разработка

```bash
# Запустить dev сервер
pnpm dev

# Запустить с Turbopack
pnpm dev --turbo

# Сборка для production
pnpm build

# Запустить production сервер
pnpm start
```

### Качество кода

```bash
# Проверить код на ошибки
pnpm lint

# Автоматически исправить ошибки
pnpm lint:fix

# Отформатировать код
pnpm format

# Проверить форматирование
pnpm format:check

# Проверить типы TypeScript
pnpm type-check

# Полная валидация (lint + format + types)
pnpm validate
```

### База данных

```bash
# Создать миграцию
pnpm exec prisma migrate dev --name migration_name

# Применить миграции
pnpm exec prisma migrate deploy

# Открыть Prisma Studio
pnpm exec prisma studio

# Сгенерировать Prisma Client
pnpm exec prisma generate

# Сбросить БД (осторожно!)
pnpm exec prisma migrate reset
```

### Docker

```bash
# Запустить все сервисы
docker-compose up -d

# Запустить с pgAdmin
docker-compose --profile admin up -d

# Остановить
docker-compose down

# Посмотреть логи
docker-compose logs -f

# Остановить и удалить данные
docker-compose down -v
```

### Линтинг и форматирование

```bash
# Проверить код
pnpm lint

# Исправить ошибки
pnpm lint --fix

# Форматировать код (если установлен prettier)
pnpm exec prettier --write .
```

## Структура проекта

```
devdigest/
├── app/                    # Next.js App Router
│   ├── (auth)/            # Страницы аутентификации
│   ├── (dashboard)/       # Защищенные страницы дашборда
│   ├── api/               # API роуты
│   └── actions/           # Server Actions
├── components/            # React компоненты
│   ├── ui/               # Базовые UI компоненты (shadcn/ui)
│   ├── dashboard/        # Компоненты дашборда
│   └── channels/         # Компоненты для работы с каналами
├── lib/                   # Утилиты и библиотеки
│   ├── auth.ts           # NextAuth.js конфигурация
│   ├── db.ts             # Prisma клиент
│   ├── telegram/         # Telegram API клиент
│   ├── ai/               # OpenAI интеграция
│   └── parsers/          # RSS и Telegram парсеры
├── prisma/
│   └── schema.prisma     # Схема базы данных
├── docker-compose.yml    # Docker конфигурация
├── .env.example          # Пример переменных окружения
└── package.json
```

## Документация

- [Руководство по настройке](SETUP_GUIDE.md)
- [UI/UX рекомендации](UI_GUIDELINES.md)
- [Качество кода (ESLint, Prettier, Lefthook)](CODE_QUALITY.md)
- [Асинхронная генерация саммари (BullMQ)](ASYNC_SUMMARY_GENERATION.md)

## Особенности реализации

### Next.js 16 Features

- **Partial Prerendering (PPR)**: Статический контент + динамические данные
- **Server Actions**: Серверная логика без API роутов
- **Cache Components**: Директива `use cache` для оптимизации
- **Turbopack**: Быстрая сборка в development

### Аутентификация

- NextAuth.js v5 с Credentials Provider
- JWT сессии
- Защита роутов через middleware
- Хэширование паролей с bcrypt

### AI Суммаризация

- GPT-4 для генерации саммари
- **Асинхронная генерация** — BullMQ + Redis, запрос возвращает 202, polling статуса джоба ([подробнее](ASYNC_SUMMARY_GENERATION.md))
- Автоматическое извлечение тем
- Кэширование результатов
- Группировка по категориям

### Автоматизация

- Cron jobs для фоновых задач
- Автоматический фетчинг постов (каждые 6 часов)
- Генерация ежедневных саммари (20:00)

## Переменные окружения

| Переменная           | Описание                     | Пример                                     |
| -------------------- | ---------------------------- | ------------------------------------------ |
| `DATABASE_URL`       | PostgreSQL connection string | `postgresql://user:pass@localhost:5432/db` |
| `NEXTAUTH_SECRET`    | Секретный ключ для NextAuth  | Сгенерировать: `openssl rand -base64 32`   |
| `NEXTAUTH_URL`       | URL приложения               | `http://localhost:3000`                    |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота          | От @BotFather                              |
| `OPENAI_API_KEY`     | OpenAI API ключ              | `sk-...`                                   |
| `REDIS_URL`          | Redis connection string      | `redis://localhost:6379`                   |

## Решение проблем

### Docker контейнеры не запускаются

```bash
# Проверить логи
docker-compose logs postgres

# Перезапустить
docker-compose restart

# Полная переустановка
docker-compose down -v
docker-compose up -d
```

### Ошибки Prisma

```bash
# Очистить и пересоздать клиент
rm -rf node_modules/.prisma
pnpm exec prisma generate

# Сбросить БД (удалит все данные!)
pnpm exec prisma migrate reset
```

### NextAuth не работает

1. Проверьте `NEXTAUTH_SECRET` в `.env.local`
2. Убедитесь что `NEXTAUTH_URL` правильный
3. Проверьте что миграции применены

## Roadmap

### MVP (Фаза 1) - Недели 1-2

- ✅ Настройка проекта и Docker
- ✅ Аутентификация (NextAuth.js v5)
- ⬜ Интеграция Telegram/RSS
- ⬜ AI саммаризация
- ⬜ Базовый дашборд

### Улучшения (Фаза 2) - Недели 3-4

- ⬜ Поиск по саммари
- ⬜ Экспорт в PDF/Markdown
- ⬜ Email уведомления
- ⬜ PWA с оффлайн режимом

### Дополнительно (Фаза 3)

- ⬜ Публичные ссылки на саммари
- ⬜ Рекомендации каналов
- ⬜ Коллекции и группировка
- ⬜ Комментарии к постам

## Лицензия

MIT

## Автор

Разработано с использованием Next.js 16 и Claude Code

---
