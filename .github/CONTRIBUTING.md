# Руководство для контрибьюторов

Спасибо за интерес к проекту DevDigest Tracker! Это руководство поможет вам начать работу.

## Подготовка окружения

### 1. Форк и клонирование

```bash
# Форкните репозиторий на GitHub, затем клонируйте свой форк
git clone https://github.com/YOUR_USERNAME/devdigest-tracker.git
cd devdigest-tracker

# Добавьте upstream remote
git remote add upstream https://github.com/ORIGINAL_OWNER/devdigest-tracker.git
```

### 2. Установка зависимостей

```bash
# Установите pnpm если еще не установлен
npm install -g pnpm

# Запустите автоматическую настройку
./scripts/setup.sh

# Или вручную:
pnpm install
docker-compose up -d
cp .env.example .env.local
pnpm exec prisma migrate dev
```

### 3. Настройка IDE

Мы рекомендуем VS Code с установленными расширениями из `.vscode/extensions.json`.

## Workflow разработки

### Создание новой ветки

```bash
# Обновите main
git checkout main
git pull upstream main

# Создайте feature ветку
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

### Разработка

1. **Пишите код** следуя стайл-гайду проекта
2. **Коммитьте часто** с понятными сообщениями
3. **Проверяйте код** перед коммитом

```bash
# Git hooks автоматически запустят проверки
git add .
git commit -m "feat: add new feature"

# Или запустите проверки вручную
pnpm validate
```

### Commit Message Format

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>: <description>

[optional body]

[optional footer]
```

**Types:**

- `feat` - Новая функциональность
- `fix` - Исправление бага
- `docs` - Изменения в документации
- `style` - Форматирование, отсутствие точек с запятой и т.д.
- `refactor` - Рефакторинг кода
- `perf` - Улучшение производительности
- `test` - Добавление тестов
- `chore` - Изменения в сборке, зависимостях и т.д.

**Примеры:**

```bash
git commit -m "feat: add telegram channel parsing"
git commit -m "fix: resolve authentication issue in middleware"
git commit -m "docs: update setup guide with docker instructions"
git commit -m "refactor: extract summary generation to separate service"
```

### Создание Pull Request

1. **Push в свой форк**

```bash
git push origin feature/your-feature-name
```

2. **Создайте PR на GitHub**
   - Заполните шаблон PR
   - Опишите что изменили и зачем
   - Прикрепите скриншоты если изменения затрагивают UI
   - Упомяните связанные issues

3. **Ответьте на ревью**
   - Исправьте замечания
   - Push изменения в ту же ветку
   - PR обновится автоматически

## Стандарты кода

### TypeScript

- Используйте строгую типизацию
- Избегайте `any`, используйте `unknown` или generics
- Используйте `type` для импорта типов

```typescript
// ✅ Хорошо
// ❌ Плохо
import { User, type User } from "./types";

function processUser(user: User): void {
  // ...
}

function processUser(user: any) {
  // ...
}
```

### React Components

- Используйте функциональные компоненты
- Предпочитайте Server Components где возможно
- Используйте `"use client"` только когда необходимо

```typescript
// Server Component (по умолчанию)
export default async function Page() {
  const data = await fetchData();
  return <div>{data}</div>;
}

// Client Component (когда нужны хуки или интерактивность)
'use client';

import { useState } from 'react';

export function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}
```

### Server Actions

- Всегда используйте `'use server'` директиву
- Валидируйте входные данные с Zod
- Обрабатывайте ошибки

```typescript
"use server";

import { revalidatePath } from "next/cache";
import { z } from "zod";

const schema = z.object({
  name: z.string().min(1),
  url: z.string().url(),
});

export async function addChannel(formData: FormData) {
  try {
    const data = schema.parse({
      name: formData.get("name"),
      url: formData.get("url"),
    });

    // Логика...

    revalidatePath("/channels");
    return { success: true };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
```

### Стилизация

- Используйте Tailwind CSS utility classes
- Используйте `cn()` для условных классов
- Следуйте дизайн-системе из UI_GUIDELINES.md

```typescript
import { cn } from '@/lib/utils';

export function Button({ className, variant = 'default' }) {
  return (
    <button
      className={cn(
        'px-4 py-2 rounded-lg transition-colors',
        variant === 'primary' && 'bg-primary-500 text-white',
        variant === 'secondary' && 'bg-secondary-500 text-white',
        className
      )}
    >
      Click me
    </button>
  );
}
```

## Тестирование

Перед созданием PR убедитесь что:

- [ ] Код проходит линтинг: `pnpm lint`
- [ ] Код отформатирован: `pnpm format`
- [ ] Типы проверены: `pnpm type-check`
- [ ] Проект собирается: `pnpm build`
- [ ] Все изменения протестированы вручную

```bash
# Полная валидация
pnpm validate && pnpm build
```

## Что можно улучшить?

### Приоритетные задачи

См. [DEVELOPMENT_PLAN.md](../DEVELOPMENT_PLAN.md) для полного списка функций.

Хорошие задачи для первого вклада (good first issue):

- Улучшение документации
- Исправление опечаток
- Добавление тестов
- Улучшение UI компонентов
- Оптимизация производительности

### Идеи для новых фич

- Экспорт саммари в PDF/Markdown
- Push уведомления
- Поиск по саммари
- Интеграция с Twitter/X
- Мобильная PWA версия

## Получение помощи

- 💬 Создайте [Discussion](https://github.com/YOUR_REPO/discussions) для вопросов
- 🐛 Создайте [Issue](https://github.com/YOUR_REPO/issues) для багов
- 📖 Читайте документацию в папке проекта

## Code Review

Ваш PR будет проверен мейнтейнерами. Мы обращаем внимание на:

- **Качество кода**: Соответствие стандартам проекта
- **Тестирование**: Все ли работает корректно
- **Документация**: Обновлена ли документация если нужно
- **Производительность**: Нет ли проблем с производительностью
- **Безопасность**: Нет ли уязвимостей

## Лицензия

Создавая PR, вы соглашаетесь что ваш код будет распространяться под MIT лицензией проекта.

---

**Спасибо за ваш вклад! 🎉**
